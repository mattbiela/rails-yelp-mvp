<%= simple_form_for(@restaurant) do |form| %>
  <%= form.input :name %>
  <%= form.input :address %>
  <%= form.select :category, options_for_select(Restaurant::CATEGORIES), prompt: "Which category?" %>
  <%= form.button :submit %>
<% end %>
